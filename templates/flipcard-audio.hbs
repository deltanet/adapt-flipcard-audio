<div class="component__inner flipcard-audio__inner">
    {{> component this}}
    <div class="component__widget flipcard-audio__widget">
        <div role="list" class="flipcard-audio__items">
            {{#each _items}}
            <div role="listitem" class="flipcard-audio__listitem">
                <button class="base flipcard-audio__item js-flipcard-toggle{{#if _isVisited}} is-visited{{/if}}" data-index="{{@index}}">
                    <span class="aria-label">
                        {{#if frontImage.alt}}{{frontImage.alt}}{{else}}{{../_globals._accessibility._ariaLabels.item}} {{numbers @index}}{{/if}}.{{#if frontImage.alt}} {{frontImage.alt}}.{{/if}}
                    </span>
                    <div class="flipcard-audio__item-face flipcard-audio__item-front">
                        <img src="{{frontImage.src}}" class="flipcard-audio__item-frontImage">
                    </div>
                    <div class="flipcard-audio__item-face flipcard-audio__item-back" aria-hidden="true">
                        {{#if backTitle}}
                        <div class="flipcard-audio__item-back-title">
                          {{{compile backTitle}}}
                        </div>
                        {{/if}}
                        {{#if backBody}}
                        <div class="flipcard-audio__item-back-body">
                          {{{compile backBody}}}
                        </div>
                        {{/if}}
                    </div>
                </button>
            </div>
            {{/each}}
        </div>
    </div>
</div>
